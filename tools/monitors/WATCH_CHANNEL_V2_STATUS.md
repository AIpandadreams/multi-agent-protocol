# watch_channel.v2.sh — LIVE STATUS MARKER (read this before trusting the script's own header)

**v2 is LIVE and RATIFIED: a fleet ruling, 2026-08-09 14:2x -0400 — the principal first-hand clickable,
"Swap all three edges now"** (`memory/orchestrator/auth-log.md`, the one-act restart the
script's header lines 6-8 reserved to him). Execution map at that entry: orch edge armed
that sitting; owner's E1193 pre-arm ratified; builder's edge dispatched by the orchestrator.

⛔ **The script's own header still reads "THIS IS A PROPOSAL AND IS NOT LIVE" — that text is
STALE**, found in production by owner E1252 (2026-08-10, v2-token grammar attribution in two
seats' obs records). It misled two seats in one night (owner E1252, and a builder entry).

**Why the header is not yet amended in place:** the live v2 bodies (orch + builder edges)
hold the file open — an atomic `os.replace` swap was attempted 2026-08-10 06:5x by orch and
refused with `WinError 5`. In-place editing a script that running bashes are executing is
forbidden anyway ([[live-instruments-must-be-frozen-or-swapped]]). The header amendment
lands at the next window in which EVERY executing body is down (a coordinated fleet watcher
cycle or machine restart) — owner's single-edge cycle does NOT unhold it, since the holders
are the orch and builder edges.

This marker file is the interim record at the claim site's nearest safe neighbor. Delete it
when the header itself is amended.

— orch, 2026-08-10 (an orchestrator batch)
