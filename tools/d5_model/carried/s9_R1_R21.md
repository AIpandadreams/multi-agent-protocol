- **R1** — token-holder exposure: any local process running as the principal can read the token and
  mint entries. **RULED (a fleet ruling): ACCEPTED, no mitigation leg** (§5.1a). Accepted is not
  resolved — the exposure exists and is carried. §11.4 builds the one factor well; it does
  not add a second.
- **R2** — the gate is only as good as `parse.py`, **plus** §2's union rule for fence-blind
  consumers. Accepted deliberately; §2.4's consumer agreement makes divergence loud, not
  impossible.
- **R3** — preview→append validity window. Now bounded by an ENFORCED mechanism (§4.1e's
  authenticated preview binding, validated under the lock — v6 stated the bound and
  carried no mechanism, v6-round F1/C1): a preview binds to a lane-date and the complete
  lane state anchor; divergence is a named `PREVIEW_STALE` refusal, never a silent
  re-derivation.
- **R4** *(v1)* — **CLOSED by v2 §7** (custody: uncommitted-entry pickup window; v1 treated
  it as transient, v2 measured it as steady-state and §7 made custody part of the path).
  Disposition recorded 2026-08-13; source `D5_V2_WRITE_PATH_DESIGN_v2.md:434`.
- **R5** — confusables/bidi. **DESIGNED at §6.3** (was "owed before build" v2→v5). The
  irreducible remainder — visible homoglyphs — is stated there as out of any byte gate's
  reach.
- **R6** *(v2)* — the tray canary as load-bearing write-confirm (canary down ⇒ writes
  refuse). **DISSOLVED in v3** when opus showed the canary has no input channel and §5
  withdrew it from the write path entirely; a residual about a mechanism the design no
  longer contains. Disposition recorded 2026-08-13; source
  `D5_V2_WRITE_PATH_DESIGN_v2.md:439`.
- **R7** — CORS preflight policy. **DESIGNED at §11.3** (was "owed"): no CORS surface
  exists at all — no ACAO header ever, OPTIONS ⇒ 403, same-origin by construction.
- **R8** — absent `Origin`. **DESIGNED at §11.3**: required + exact-match on state-changing
  routes; absent ⇒ 403.
- **R9** — **DISSOLVED** (§3.1). Replaced by **R9′**: we can now block peers, so lock scope
  must be minimal and must never span a subprocess or git call (§7.1b step 1 and §12 honor
  this by construction).
- **R10** *(v2)* — the concurrency probe measured one host/Python/filesystem; nothing may
  be tuned to its figures. **ABSORBED into §3.2's categorical-response rule** ("nothing in
  this design is tuned to these figures") — restated here so the number's disposition is
  findable. Source `D5_V2_WRITE_PATH_DESIGN_v2.md:453`.
- **R11** — §2.2's REFUSE tier rejects legitimate appends quoting an entry heading in a
  code fence. Named cost, taken deliberately, ruled breadth by a fleet ruling. The hand-paste
  workaround no longer bricks the lane (§4.1d `FOREIGN_COMPLETE`), which removes the
  sharpest edge opus v5 O5 found on this cost.
- **R12** — Win32 share modes / per-component no-follow / post-write revalidation.
  **DESIGNED at §6.2** (was "owed"). The battery legs that test the contract remain build
  artifacts (§6.1's list).
- **R13** *(new in v6; the residual v5's fold table claimed §9 held and §9 did not)* — a
  torn orphan tail containing a heading-shaped line stays LIVE for fence-blind consumers
  until the principal rules on the bytes (§4.1f). Detection (§4.1j), supersession (§4.1f), and WARN
  coverage (§2.3) route around it; only a truncation this design declines would remove it.
  **Open; escalation path: §13.4, disposition `escalated`.**
- **R14** *(new)* — class-C detection is RULED (read-time first, §4.1j) but its hot-path
  cost is a design expectation, not a measurement. If build-time measurement finds it
  unaffordable, that REOPENS the orchestrator ruling's §2(i) — the reopen clause is part of the ruling and
  of §4.1j.
- **R15** — ⭐ **RULED at the v9 authoring: the custody trigger is ALTERNATE A,
  automatic-with-bounds (a fleet ruling, the principal first-hand clickable 2026-08-13 ~15:1x ET;
  relayed by the orchestrator, verified against the orchestrator auth-log tail at this authoring —
  the v8 authoring's 14:30 check predated the ruling and correctly found none).** This
  entry carried F8's deleted-mitigation half (§5.1a) — alternate B was that mitigation,
  alternate A declines it — and the ruling decides it WITH the tradeoff on the ruled
  package's face: the deleted human act stays deleted, by his word. What R15 still
  carries, so closure is not overstated: **custody remains DISABLED until this artifact
  passes its own freeze round (condition (ii))**, and the §5.2 custody-revert repair path
  (condition (i)) is part of the ruled text — a reviewer who finds it underspecified is
  finding against a ruling condition, which is a REJECT-grade finding here.
- **R16** *(new in v10)* — the §4.1e pre-boundary EPOCH-CANDIDATE ANOMALY class is
  deliberately NARROWED to lines whose first five bytes are `<<CO-` or whose entire
  content is a bare 1–4-byte prefix (`<`, `<<`, `<<C`, `<<CO`) — classifying EVERY
  pre-boundary line starting `<` would quarantine ordinary legacy content wholesale
  (v9-round C1's cure, bounded by this seat — §10.2 (q)). The residue: a tear inside
  the FIRST FOUR bytes of the first-ever envelope write, whose journal record is ALSO
  lost, later TERMINATED BY FUSED PEER BYTES, is byte-indistinguishable from legacy
  content and reads as pre-epoch. §7.1c's INTENT-emission comparison (v9-round C1)
  catches the tear whenever the journal survives; the blind window requires the
  CONJUNCTION of a ≤4-byte tear, journal loss, and a peer termination fused onto the
  residue. Open; narrow by construction, and disclosed rather than papered over.
- **R17** *(new in v11; UPDATED in v12)* — the `custody-delta-overflow` exit (§5.2,
  v10-round O3) is a
  FIRST-HAND act: from the moment a lane's delta crosses 4 MiB until the
  `custody-delta-override` disposition, custody for that
  lane is refused and owed-and-visible — human latency, hours to days on the §4.1i
  traffic evidence. The wedge has a named exit, but the exit is not automatic, by
  design (the object is the principal's authority surface). ⛔ **v12 narrows this residual by
  REMOVING v11's secondary "bounded partial-snapshot commit" arm (v11-round C8: it
  specified neither record-selection nor journal transitions), so the override is the
  ONLY exit and the human-latency window is the whole of the cost — no second,
  half-specified path shares it.** ⛔ **v13 (v12-round X4) additionally moves the
  override's CONSUMPTION record out of git reachability into
  `ops/write_path/override_ledger.log`, which narrows nothing about the human-latency
  cost but closes the re-arm/burn pair; the ledger's own residual is R22 below.**
  ⛔ ⭐ **AND IN v15 THIS RESIDUAL'S EXIT IS RESTORED, HAVING BEEN UNREACHABLE IN v14 —
  DISCLOSED, BECAUSE A RESIDUAL WHOSE EXIT DOES NOT EXIST IS NOT A RESIDUAL, IT IS A WEDGE
  (v14-round Q2≈Z8, opus REJECT-grade): v14 declared an absent `override_ledger.log`
  anomalous on every non-fresh install and REFUSED ALL WAIVING while it was absent — and
  NOTHING IN v14 EVER CREATED THAT FILE. So from the second start of every ordinary
  install, the ONLY EXIT THIS RESIDUAL NAMES was refused, and an install that tripped the
  4 MiB ceiling had NO EXIT AT ALL — the permanent-wedge class §4.1f exists to condemn,
  reintroduced through the very ledger v13 added to make the override accountable. v15's
  §11.1 pre-bind pass CREATES the ledger empty with its own init marker, and
  `override-ledger-missing` is NARROWED to marker-present-ledger-absent, so the healthy
  path never raises it and the override act is available exactly when this residual says
  it is.** Open;
  escalation path: the override act itself.
- **R18** *(new in v11; REPLACED in v12 — the claim was wider than the mechanism
  defends)* — ⛔ **the MINIMAL TWO-OBJECT ROLLBACK (v11-round C6).** v11 disclosed that
  a paired restore of the FULL `ops/` tree defeats the completeness mechanisms. **That
  overstates what an attacker or a careless restore must do.** The actual minimum is
  **two objects**: roll `ops/write_path_journal/` back to an older manifest `M1` and its
  matching files, and TRUNCATE `ops/write_path/census_anchor.log` to its valid prefix
  ending at the line `(seq1, sha256(M1))`. Every check in §13.5 then passes — strict
  increase holds on a prefix, the last line matches the restored manifest, the manifest
  `seq` is not less than the ledger's, per-class equality holds — and a later `J_B`
  disappears with no census verdict. **`block_ledger.log`, `revert_auth.log` and the
  enablement generations need NOT be touched.**
  - **What still fires after that rollback, stated so the residual is not read as wider
    than it is** — ⛔ **AND CORRECTED IN v14, BECAUSE v13's LIST WAS FALSE IN THE
    FLATTERING DIRECTION (v13-round P3≈Y3): every survivor below survives ONLY WHILE ITS
    OWN FILE IS PRESENT, and v13 stated the block-ledger limb as an unconditional
    survival while §11.1 silently replaced a REMOVED ledger with an empty one. The
    survivors are conditional, and the condition is now written into each:**
    - **any post-`M1` OPEN BLOCK in `block_ledger.log` survives and keeps its lane blocked
      with a missing sidecar (§13.4) — ⛔ PROVIDED `block_ledger.log` IS ITSELF PRESENT.**
      **The block ledger protects only while it itself exists.** A restore or a removal
      that takes the ledger takes the protection with it — and under v13 that loss was
      SILENT (the pre-bind pass created an empty replacement, which reads as "no open
      blocks"). **In v14 it is LOUD**: the ledger is re-created only on a provably fresh
      install, the `BLOCK_LEDGER_INIT.json` marker makes a post-removal absence
      detectable, and §13.4's `block-ledger-missing` fires — blocking every lane carrying
      a `no`-class sidecar and suspending the custody stage. **That does not make the
      ledger survive the rollback; it makes its NON-survival observable**, which is the
      honest claim and the only one this design can make [[disclosure-with-wrong-bound-keeps-defect-alive]];
    - any post-`M1` revert AUTHORIZATION in `revert_auth.log` survives **while that file is
      present**, so a landed revert whose `REVERT_*` record vanished is still resolvable and
      a `revert-auth-unused-generation` item fires — ⛔ **and its ABSENCE is governed by
      the MISSING-FILE RULE STATED AT §5.2 CLAUSE 7, which is a rule that now EXISTS
      (⛔ CORRECTED IN v15, v14-round Q9: this sentence read "its own startup-integrity and
      DAMAGED-FILE rules (§5.2) govern its absence" — and §5.2's damaged-file rules KEY ON
      DAMAGE. There was NO missing-file rule for `revert_auth.log` anywhere in v14. The
      citation made the gap look DISCHARGED, which is worse than an open gap, because a
      cited rule is not re-checked. Same shape as the override ledger's, one file over).
      The rule, now written at clause 7: §11.1's pre-bind pass CREATES the file empty with
      a `REVERT_AUTH_INIT.json` marker; MARKER-PRESENT-AND-FILE-ABSENT ⇒
      `revert-auth-ledger-missing`, and because that ledger is clause 7's AUTHORITATIVE
      reading, an absent one resolves NO live authorization and EVERY REVERT REFUSES with
      `revert-unauthorized` — fail-closed for an act that rewrites the principal's authority
      record. The exit is a first-hand re-init, which retires every outstanding
      generation**;
    - **any post-`M1` OVERRIDE CONSUMPTION in `override_ledger.log` survives, so a spent
      override cannot be re-armed by this rollback either (v13)** — ⛔ **and in v14 its
      ABSENCE is no longer unspecified: `override-ledger-missing` treats every listed
      override as CONSUMED and refuses waiving until a first-hand act (§5.2, v13-round
      P5≈Y5), so the fail-closed direction holds whether the file survives or not**;
    - any post-`M1` ENABLEMENT generation survives and takes census row 7 **while its file
      is present**; its listed-and-missing case is §11.1's fail-closed arm.
    ⚠ **The general form, stated once so no future survivor is written unconditionally:**
    every out-of-domain object in this list is a FILE, and a rollback wide enough to take
    it takes what it protects. **What this design owes at each is not survival but
    DETECTION** — a named missing-file rule that fires fail-closed — and every one of the
    four now has one.
  - ⛔ **THE SILENT CLASS — RESTATED AND WIDENED IN v13 (v12-round P6, and the finding is
    about the word "precisely": v12 wrote "the silent class is precisely `J_*` records and
    `DISPOSED_*` files with no out-of-domain trace", and an incomplete enumeration
    asserted as complete is exactly what R18 was rewritten to stop being).** The silent
    class is:
    - **`J_*` journal records** with no out-of-domain trace — owed custody for lanes that
      had no `no`-class incident, no revert and no enablement act in the window;
    - **`DISPOSED_*` files** with no out-of-domain trace — a lifted block reverts to
      blocked or a resolution is forgotten, depending on which side of `M1` it fell;
    - ⛔ **`SIDECAR_*` files of `re-mintable-by-full-scan: `yes`` kinds** —
      `TORN_REPORT` and `READTIME_DETECT` — **created after `M1`.** They carry NO block-
      ledger line, by design: §13.4 exempts them on the ground that the full scan
      re-mints them from the lane bytes. **Under this rollback the census PASSES and NO
      SCAN EVER FIRES**, so they are simply gone.
      **Bounded consequence, stated exactly (this is why the entry is a widening of the
      disclosure and not a new hole):** the LANE'S OWN BYTES are untouched by an `ops/`
      rollback, so §4.1d still classifies the problem span `UNSETTLED` and **the write
      gate still REFUSES the lane** — the protective behaviour survives. **What is lost is
      the BLOCKED/health SURFACE** (the operator sees a refusing lane with no named
      incident) **and the sidecar the correction path resolves against** (§4.1f step 2's
      `torn_report` names a stem that no longer exists, so the repair must wait for the
      next read-time or write-time detection to re-mint one — which it will, on the next
      touch of that lane, because the evidence IS the bytes). The window is therefore
      "loud refusal without a named incident until the next detection", not "silent
      acceptance".
  - **No local mechanism defeats it.** The only real defense is an off-machine reference
    ([[shared-machine-needs-off-machine-reference]] — e.g. the git remote's copy of
    committed custody state, which a local restore cannot rewind); building that
    comparison is NOT designed here, and a third local high-water file would only move
    the truncation target.
  - ⚖ **ACCEPTED under the R1 ruling's class, per the orchestrator's ruling — revisit trigger:
    phone-ruling-lane live** (or any threat-model change; the trigger travels with this
    row per the ruling). The acceptance covers exactly the honest width stated above —
    never the wider claim v11 made [[disclosure-with-wrong-bound-keeps-defect-alive]].
    The off-machine reference stays the NAMED CURE if the trigger fires; it is not
    commissioned now. Closed via ACCEPTANCE ITEM A1 below (v13, seat-folded).
- **R19** *(new in v12; ⛔ RESTATED HONESTLY in v13 — the claim was wider than any
  mechanism enforces)* — the revert AUTHORIZATION binding (§5.2 clause 7,
  `ops/write_path/revert_auth.log`).
  - ⛔ **What v12 claimed and why it was wrong (v12-round X2):** v12 said the binding
    "defeats a hand-written revert commit … a forger cannot append to a ledger only
    §12's worker writes." **No mechanical boundary enforces that.** The ledger lives in
    `ops/write_path/`, whose files are WRITABLE BY <USER>'S PRINCIPAL (§11.4) — the same
    principal this design already assumes can read the token and operate on the
    repository. **"§12-worker-only" is a WRITER CONVENTION, not authentication.** A
    principal-level actor can write the commit AND forge the matching
    `authorize`/`attempt`/`consume` lines.
  - **What the mechanism actually buys, stated at the honest width:** it constrains
    **ACCIDENT** (a stray or mistaken revert-shaped commit has no ledger evidence and is
    refused, loudly, as a `REVERT_VALIDATION` incident) and **OFF-PRINCIPAL tampering**
    (anything that can write the repo but not `ops/write_path/` — a different user, a
    process under a different account, a repo-only exfiltration or restore path). It also
    raises the DELIBERATENESS bar for a principal-level forgery: the forger must now
    fabricate four coherent out-of-domain lines including a `tree`/`parent` pair matching
    the commit it built, rather than copy two public trailers.
  - **What it does NOT buy, said plainly:** it does not authenticate the machine
    principal, and **it does not stop a principal-level hand-written revert.** That
    remains possible and is disclosed here as this residual's honest bound. It is the
    **SAME TRUST CLASS as R1** — the token-holder exposure the principal ACCEPTED at **the R1 ruling**
    (§5.1a) — reaching one surface further, and it is stated as such rather than dressed
    as a defeat [[stated-reason-must-discriminate]] [[sound-about-what-it-names-unsound-about-what-it-licenses]].
  - **Bounded by what a revert can do:** withdraw a custody claim over bytes that remain
    on the append-only lane. It cannot remove bytes, rewrite history, or reach any path
    outside the lane (clause 9).
  - ⚖ **ACCEPTED at the WIDENED width under the R1 ruling's class, per the orchestrator's ruling — revisit
    trigger: phone-ruling-lane live** (or any threat-model change; the trigger travels
    with this row per the ruling). The ruling's ground: this is "the same sentence aimed
    at the revert path instead of the append path — same actor, same capability, already
    seen and accepted." Closed via ACCEPTANCE ITEM A1 below (v13, seat-folded).
- ⭐ **ACCEPTANCE ITEM A1 *(new in v13)* — ONE routed item, covering the ON-MACHINE TRUST
  CLASS, for R18 AND the widened R19.** Both residuals are the same question wearing two
  masks: **what a principal that can write this machine's `ops/` tree can do to this
  path's own evidence.** R18 is that principal (or a careless restore) ROLLING BACK the
  completeness evidence — two objects, no third required. R19 is that principal FORGING
  the revert-authorization evidence — the commit and its ledger lines together. Neither
  has a local mechanical defense that does not move the same target
  [[shared-machine-needs-off-machine-reference]], and neither is claimed to have one.
  - **What is asked:** a first-hand acceptance (or a direction to build the off-machine
    reference) covering **BOTH**, at the widths stated in R18 and R19 above and nowhere
    narrower.
  - **Routing:** the authority lane — the principal first-hand, or orch-relayed with a resolvable
    auth id (the fleet's single-point routing rule), exactly as the R1 acceptance was taken.
  - **Why ONE item and not two:** they share a mechanism, a principal and a cure, and
    splitting them invites accepting one and reading the other as covered — the failure
    [[disclosure-with-wrong-bound-keeps-defect-alive]] names. **A CONFIRM on this artifact
    does not constitute that acceptance** (§10): the item is disclosed here so the round
    can weigh it, and it stays open until the authority lane answers it — which, per the
    next bullet, the lane has now done.
  - ⚖ **CLOSED against the orchestrator's ruling — SEAT-FOLDED, with the lineage preserved.** The
    authoring leg found the lane's tail at **that ruling** (unstamped, `STAMP_AT_APPEND`;
    the orchestrator lane), postdating the earlier ruling's pin §11.1
    carries, and — per its instruction to REPORT a newer authority entry rather than fold
    one silently — left A1 OWED and the ruling unapplied. **The builder seat then verified
    that ruling at source (line 527 of the lane file) and folded it:** R18 and R19 are
    both **ACCEPTED under the R1 ruling's already-ruled on-machine-principal class** ("not
    re-asked" — both residuals are instances of the accepted class, not new exposures),
    with the revisit trigger **"the phone ruling lane going live (or any threat-model
    change)"** travelling with both rows, exactly as the ruling mandates. The off-machine
    reference remains the NAMED CURE if the trigger fires and is not commissioned now.
    The principal sees the ruling in the 23:00 sitrep as FYI-with-lineage. A1 is therefore
    **CLOSED**; what a CONFIRM on this artifact does and does not constitute (§10) is
    unchanged — the acceptance came from the authority lane, not from any review verdict.
- **R20** *(new in v12)* — §13.4's ledger DEDUP exit: when an identical `no`-class
  incident is re-observed on a stem whose block was previously RELEASED, and the sidecar
  file on disk is byte-identical in identity to the one that was dispositioned, the
  worker auto-appends a matching `release` (with a `block-ledger-reopen-auto-released`
  health item) rather than re-opening the block. **The cost: a genuine RE-OCCURRENCE of
  the same incident identity is discharged by the earlier disposition rather than
  re-escalated.** It is bounded by immutability (the sidecar file, hence the incident
  identity, is the same object) and it is loud (the health item), and the alternative —
  a first-hand act to close a block nobody opened — routes work around the guard, which
  is the failure class [[unusable-guard-routes-work-around-itself]]. Open; escalation
  path: the health item, dispositionable under §13.4.
- **R21** *(new in v12; ⛔ UPDATED in v13 — the escalation path now names a MECHANISM)* —
  §11.1 window (ii): a crash between an enablement record's
  `CREATE_NEW` and the manifest replace leaves a valid record present-but-unlisted, and
  §13.5 census row 7 ADOPTS it while **suppressing new custody commits until a first-hand
  confirmation**. The server cannot distinguish its own crash-window record from one
  placed on disk by any local process (R1's class), so it refuses to infer authority from
  bytes it did not observe itself writing. **The cost is real and priced here: a crash in
  a sub-second window costs one first-hand confirmation act before automatic custody
  resumes.** The alternative — adopting the recorded authority silently — would let a
  file on disk enable automatic custody over the principal's authority record, which is the exact
  act the orchestrator's earlier ruling reserves. ⛔ **AND THE CONFIRMATION ACT IS NOW PERFORMABLE (v12-round
  P2≈X5: v12's R21 pointed at "a first-hand disposition of the health item" and §11.1's
  creation gate forbade the alternative — a fresh generation — outright, so BOTH exits
  from this state were unperformable and the residual priced a cost the design could not
  actually pay): the exits are (a) a fresh generation `g<N+1>` under §11.1 rule **(B)(a)**,
  or (b) a `custody-enablement-confirm` disposition (§13.4's enum value over §13.1's
  reserved stem `custody-enablement-confirm‖<host>`, naming the generation in
  `confirms`) — **which is §11.1 rule (B)(b).** ⛔ **AND IN v14 BOTH EXITS ARE ACTS, WHICH
  v13's WERE NOT (v13-round P2): v13 pointed exit (a) at "rule (B)(ii)", i.e. at THIS
  RESIDUAL'S OWN OPEN HEALTH ITEM being the trigger — so the state that prices a
  first-hand confirmation would have discharged itself at the next start, either silently
  regenerating the enablement record (the O2 defect the orchestrator's ruling forbids) or naming an act
  with no mechanism. The item is now the PRECONDITION and the two acts are the triggers,
  so the cost this residual prices is a cost the design can actually charge and the
  operator can actually pay.** Open by design; escalation path: the
  `custody-enablement-adopted` health item, dispositionable by (b).
