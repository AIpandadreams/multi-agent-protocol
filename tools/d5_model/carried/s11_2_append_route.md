| `/api/append` | POST | token | `{id, ts, n, gist, body, pv, pv_mac}` → `{outcome, journal_ref, warnings[]}` — outcome is a §4.1d row name or a named refusal cause (`PREVIEW_STALE`, `binding-invalid`, `id-mismatch`, `body-carriage-return`, `lane-lock-timeout`, `lane-blocked`, …). The binding fields are schema fields, not unknown fields; the server validates `pv_mac`, then the live state under the lock, then recomputes and compares the emission (§4.1e). A `pv` whose `correction` block is non-null is REFUSED on this route (`400 wrong-route`) — correction bindings land only via the correction route |
