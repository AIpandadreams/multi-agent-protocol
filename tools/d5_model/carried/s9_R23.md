- **R23** *(new in v13)* — **the collision-intake handoff widens the read path's write
  surface by one append-only file.** §13.1 lets `parse.py` and the dashboard render path
  append to `ops/write_path/collision_intake.log`, which is the first out-of-domain file
  a non-worker process may write. The cure is bounded deliberately (append-only,
  single-line, evidence-only, never authority — the WORKER re-derives the sidecar
  identity from the two stems and mints the incident itself), but it is a widening and
  it is named rather than buried: a misbehaving read path can now grow that file, and a
  torn append there is a `collision-intake-damaged` health item rather than a silent
  loss. The rejected alternative — redesigning the read-time collision outcome so the
  read path needs no worker at all — would have required read paths to write
  block-ledger lines, which is a strictly larger widening of the same kind. ⛔ **AND THE
  ESCALATION PATH IS CORRECTED IN v14, BECAUSE v13's CITED A MECHANISM THAT DID NOT EXIST
  (v13-round Y7): v13 closed this residual with "escalation path: the health item,
  dispositionable under §13.4" — but §13.4 admits SIDECAR STEMS and the reserved
  no-sidecar stems, and NONE of them represented `collision-intake-damaged`. Worse, the
  damage is structurally permanent: a torn final line leaves an append-only file
  unparseable forever, and no later append can restore line framing, so the item could
  never be closed and every startup re-raised it. A residual whose escalation path does
  not resolve against any artifact is the [[names-must-resolve-against-artifacts]] failure
  inside the disclosure itself.** The escalation path is now a REAL, NAMED mechanism:
  **§13.1's QUARANTINE/ROLLOVER for the intake, invoked through the FOURTH reserved
  no-sidecar disposition stem `collision-intake-rollover‖<host>`** (§13.4's enum value,
  its `quarantine` field, the standard actor class and auth-id recording, an append-only
  rollover intent written before the rename, and the same four crash windows §13.4
  enumerates). The parseable prefix is DRAINED into the fresh file rather than discarded;
  the torn fragment's own lane is unrecoverable and is not guessed at; and **while any
  `collision-intake-damaged` item is OPEN, collision handling FAILS CLOSED GLOBALLY**
  (`collision-intake-unavailable` — a read-path race loser refuses its read). Open;
  escalation path: the health item, **dispositionable through
  `collision-intake-rollover‖<host>` under §13.4** — a stem that now exists.
