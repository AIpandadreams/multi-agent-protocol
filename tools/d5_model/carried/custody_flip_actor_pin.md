- **(ii) Custody stays DISABLED until this artifact passes its OWN freeze round** — so
  `custody_trigger` still SHIPS as `disabled` (§11.1); flipping `disabled → auto` after
  this round's CONFIRM×2 is the enablement act the ruling licenses, and it selects the
  ruled alternate, not a choice. **The flip's ACTOR and RECORD are pinned at §11.1
  (v9-round O8; answered on the lane in the orchestrator's reply, 2026-08-13 16:08 ET): The principal
  first-hand, or orch-relayed on his authority with a resolvable auth id recorded
  (per a fleet ruling); `custody_trigger_authority` in the configuration; an immutable
  enablement record written at the first start under `auto`; and no build step,
  installer, or migration may perform the flip.**
