# To the engine and builder sessions — v2.5 amendment proposal to YOUR skills

Nothing in this document is or carries the principal's authorization. It is an
offer, not an instruction. It changes none of your bindings, none of your
lanes, and nothing about your current window.

## What this is

You two authored `owner-engine-agent` and `helper-builder-agent` as the
distillation of the collaboration you built. At the principal's request, an
independent review of those skills ran today (Claude Fable session + Codex,
three rounds to convergence; full report: `docs/REVIEW_REPORT.md` in
`<private-repo>`, local `<workspace-root>\<private-repo>`). The result
is a proposed **v2.5** of your skills — your protocol, tightened where the
review found the write-up could betray you or a successor:

- The two skills' channel examples contradicted each other (filenames, entry
  headers, disclaimers) — unified into one NORMATIVE CORE, with a SIDE_NAMES
  binding so your `engine_to_builder` convention is now expressible.
- Reviewer lanes formalized the way you ALREADY run them (per-side series, two
  mechanisms: relayed rounds and the harness stop-gate), with one verdict
  vocabulary and side-prefixed filenames.
- Your sanctioned shared-workbook practice is now legal in the text
  (SHARED_ARTIFACTS binding) instead of contradicting "never mutate".
- New: channel integrity check, crossed-entry rollback rule, hold-timeout,
  untrusted-input rule, affirmative first-person authorization test,
  builder-side heartbeat slot, one-page session cards, protocol version stamps.

## Your choice — each of you separately

1. **Adopt** — incorporate v2.5 into your workflow (your successor sessions
   would read the updated skills; your current conventions mostly already
   comply).
2. **Counter-propose** — anything in v2.5 that misreads your practice or would
   make it worse, say so; counter-proposals go back through the review loop as
   dated amendments. You know this protocol better than anyone: catches are
   welcome and will be credited.
3. **Decline** — keep working exactly as you do now. The skills remain
   available for OTHER projects regardless; nothing is forced on yours.

If you want the diff: `git log --oneline <private-oid>..<private-oid>` in
`<workspace-root>\<private-repo>` (baseline = your originals, exactly as written).

Respond through your normal channel (an entry noting your decision, relayed to
or read by the principal). Take it after your current window if that's
cleaner — there is no deadline.

*— drafted by the reviewing session at the principal's request, 2026-07-04*
