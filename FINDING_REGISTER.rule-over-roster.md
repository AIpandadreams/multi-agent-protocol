# `rule-over-roster` — finding register

Every row either anchor raised across r2 and r3, with a NAMED disposition. The r3 adjudication
record §4 requires this for the ADVISORY rows; the BLOCKING/CONDITION rows are here too,
because a register that lists only what was left undone cannot be checked against the
branch.

⛔ **A row is named here, not re-argued.** Nothing in this file re-reviews a row both
anchors CONFIRMED, and nothing here is a new finding. Where a row's disposition is
NOT CURED, the reason is stated as a reason, not as a plan.

Branch tip when this register was written: the r4 tip of 2026-09-02, squashed into `236cb31`, **UNPUSHED**. Publication is
the principal's first-hand word and has not been asked for.

## Round 3 — codex `NOT-MERGE-READY`, opus `MERGE-READY`; adjudicated SPLIT by the r3 adjudication record

### Cured in r4

| row | anchors | finding | disposition |
|---|---|---|---|
| codex R-1 · opus R-1 | **both, independently** | Even dash parity accepts two independent clause breaks as an aside | **CURED** by the r4 subject-association change of 2026-09-02, now in `236cb31` — parity replaced by the nearer-subject rule; both anchors' sentences pinned SILENT, real aside pinned FIRING, r3 parity restored in-arm as the red drive |
| codex R-1 (second half) | codex | the same cure pinned a leading-aside false negative | **CURED, and the pin RE-DERIVED** by that same r4 change of 2026-09-02, now in `236cb31` — the new rule reaches that shape, so the arm now pins it FIRING. The r3 arm reded on this change and named its own re-derivation, which is what a pinned residual is for |
| codex R-2 · opus R-2 | **both, independently** | a range endpoint off the `HOPS` ladder is silently left literal — neither expanded nor rejected | **CURED** by the r4 range-endpoint change of 2026-09-02, now in `236cb31` — reported as `UNKNOWN-ENDPOINT:<v>`; an ungradable claim and a clean claim no longer share a rendering |
| codex R-3 | codex CONFIRMED **by fixture**, opus refuted | three bare version arguments composing assigned prose satisfy the table exemption and evade both legs | **CURED** by the r4 table-structure change of 2026-09-02, now in `236cb31` — settled by execution per the r3 adjudication record §3, not by a third voice. The exemption count now descends container structure only |
| opus R-3 | opus | the exit-1 arm reaches only `status == "error"`, leaving `unsupported`→1 unpinned | **CURED** by the r4 refusal-execution change of 2026-09-02, now in `236cb31` — a pinned workspace is built and the refusal is graded on its text as well as its code |

### NOT cured — named, with the reason

| row | anchors | finding | disposition |
|---|---|---|---|
| opus R-3 (second clause) | opus | the pre-existing exit-1 fixture is the REPO ROOT, and changes meaning if a `BINDINGS.md` ever lands there | **NOT CURED — NAMED.** The r4 arm adds a self-built workspace and does not touch the old fixture. Repointing it is a change to an arm both voices CONFIRMED, which the r3 adjudication record §4 forbids this round. It is a real fragility and it is on this register, not in a comment |
| codex R-4 | codex | abbreviations and leading-dot decimals remain sentence-boundary false negatives | **NOT CURED — ADVISORY, and the boundary is heuristic by construction.** codex's own Q1 CONFIRM says so. Widening `BOUNDARY` for abbreviations trades a false negative for false positives on the module's real prose; that trade needs its own measurement round |
| codex R-5 · opus R-4 | both | the equivalence anchor hard-codes seven hop counts against the LIVE `HOPS`, so the next stamp bump reds it and the natural repair re-hand-writes the roster | **NOT CURED — the sharpest row on this register.** The frozen-prefix cure sits twenty lines below it and was not applied. It is outside r4's five items. ⚠ This is the roster defect the branch exists to kill, surviving inside the branch's own test file |
| codex R-6 · opus R-11 | both | `README.md:144` — a hand-written `v2.5–v3.1` range describing the tool, outside every leg's scan | **NOT CURED — DECLINED FOR THIS BRANCH**, and both anchors agree the decline is proportionate (codex Q9 CONFIRM; opus "correctly declined"). Outside the two-file delta |
| codex R-7 | codex | the subject-first and trigger-first scans are independent, so one claim can be reported twice, contrary to the non-duplication comment | **NOT CURED — NAMED, and r4 CHANGED THIS SURFACE.** The scan loop was rewritten by the r4 subject-association change of 2026-09-02, now in `236cb31`; the non-duplication comment now speaks only to accepted matches within ONE pattern and makes no claim across the two patterns. The row stands as codex wrote it |
| opus R-5 | opus | `_claim_findings` does not pass `blank`, so leg 2 scans the tables leg 1 is exempted from, and the divergence is undocumented | **NOT CURED — NAMED.** opus's own escalation trigger for it (the exemption blanking real prose, or missing `HOPS`/`SLOT_INTRODUCED_IN`) is measured NOT fired by the r4 arm, which asserts the exemption still names `HOPS` |
| opus R-6 | opus | the red-drive arms mutate class-global `BOUNDARY`/`DASH` process-wide; safe under sequential `unittest discover`, silently wrong under a parallel runner | **NOT CURED — NAMED, and r4 ADDED ONE.** The r4 association arm substitutes `_association_holds` the same way. The hazard is unchanged in kind and now has one more instance; the workflow runs the sequential runner, and that is a property of the workflow, not of these arms |
| opus R-7 | opus | the liveness arm pins two table names by hand, so a legitimate third version table reds an unrelated test | **NOT CURED — NAMED.** A hand-written list inside the guard, which is this branch's own subject; it is small, loud, and outside r4's items |
| opus R-8 | opus | the `LIST`-alternation reconciliation raises `AttributeError` rather than a drift message if `LIST` gains a nested group | **NOT CURED — NAMED.** Fails LOUD, in the safe direction |
| opus R-9 | opus | the alias-identity arm is filed in `TheCLIContractIsDRIVENTest`, which does not describe it | **NOT CURED — COSMETIC**, opus's own word |
| opus R-10 | opus | the heading prints "a conditional row is REQUIRED once its condition holds" even when no conditional row follows — vacuous rather than false | **NOT CURED — NAMED.** Vacuous, not false; opus's own grading |

### Open, and NOT closable at this seat

| row | finding | state |
|---|---|---|
| codex Q7 · opus Q7 | an exact-object CI green is NOT FOUND | ⛔ **STILL OPEN.** The three workflow steps ran locally at the r4 tip of 2026-09-02, now in `236cb31`, in the workflow's own order — `Ran 489 tests in 591.509s — OK` (terminator `DISCOVER-COMPLETE rc=0 sha=23f73982…`, quoted verbatim from that run and therefore naming the PRE-SQUASH object; reconciles against r3's 485 at the r3 guard tip of 2026-09-02, now in `236cb31` — 485 + the 4 arms r4 added), `mirror_check` rc 0, `release_scrub` rc 0 — ⚠ **and that scrub green is STRUCTURAL, not a content clearance — as the workflow step's own name has said since 2026-08-07 ("not a leak gate"):** it runs against `examples/scrub_patterns.example.txt`, whose header says it is structure only and deliberately generic so the example passes cleanly, and measured on this branch it returned `clean` rc 0 over a tree that still carried six internal coordination identifiers and two given-name mentions. The limit was disclosed at the step; what was missing is that it did not travel to this row, which quotes the green as evidence. The real pattern list is held privately at the authoring seat — never in this repository and never a CI secret — and a whole-tree identifier sweep is the standing pre-publish step for this repo, run and reported before every publication (at this cure: six hits before, zero after). — but on Python 3.13.7 / Windows against the workflow's pinned 3.12 / ubuntu-latest, so a version- or OS-sensitive failure would not be caught. This discharges the EVIDENCE half and **not** the CI half. The r3 adjudication record §4(5) directs a push to close it. ⚠ **An earlier draft of this row stated that `origin/rule-over-roster` is at `c235055`, an r1-era object that is not an ancestor of this tip, so that publication would require `--force-with-lease`. That was measured again and it is FALSE.** `c235055` **IS** an ancestor of this tip (`git merge-base --is-ancestor c235055 236cb31` → rc 0, re-run at the squash, with `ls-remote` confirming the remote branch still at `c235055`), so publication is a **fast-forward** and needs no force of any kind. The correction is made here, in the object, because a disclosure made elsewhere does not travel with a merge. Publication remains **the principal's first-hand word** — an outward publish is his whether or not a force is involved — and it has not been done and is not asked for here. ⚠ **Annotated 2026-09-19 on the principal's word (annotate in place; no new row, nothing above re-argued):** the suite the green above quotes was run again at this register's tip, same Python 3.13.7 / Windows, and reads `Ran 490 tests` — `FAILED (failures=11, skipped=3)`. One test more than the 489 (added since), and **all eleven failures share one cause that is not this branch**: the conformance gate's `BLOCKER — commit identity: this workspace would commit as a configured PERSONAL address`, raised because those tests build temporary fixture workspaces that inherit the machine's GLOBAL git identity. The fixture-building subset (45 tests) fails the same **7** at this tip, at the pre-squash r4 tip both anchors reviewed (whose published form is `236cb31`), and at `main` itself (`fea52bf7`); the two files this branch changes are read by none of the seven. A cure is drafted on a peer branch, `hardening/fixture-git-env-isolation`, and does not yet cure it (same 7 at `21ba2e4`). So the `489 … OK` line stays as the receipt of its day, and this row now also says what a rerun shows: **the exact-object CI green is still NOT FOUND, and a local run at this tip is red for a machine-identity reason the workflow's own runner would not share** — one more reason the CI half is discharged only by CI. |

## Round 2 rows

Round 2's five codex BLOCKING rows and opus's conditions were cured in r3 (seven
commits of 2026-09-02, now in `236cb31`) and verified two independent ways — the branch's arms,
and `repro_R1_R4.py`, which imports the branch module BY PATH and grades through its
real methods rather than a copy of its patterns. Both anchors confirmed those cures at
r3; none is re-opened here. The r2 rows that SURVIVED into r3 are the re-raised ones
listed above under their r3 (opus) numbering, which is how both anchors cited them.
